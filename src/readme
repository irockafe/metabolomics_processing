To install as a package, do:
"pip install ."

To update after you've make changes (you have to do this)
"pip install --upgrade ."

Known problem:
- Want to be able to do: "import revohealthcare.util as util"
   - Currently must do: "import util"
